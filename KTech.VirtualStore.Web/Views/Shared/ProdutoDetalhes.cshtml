@model KTech.VirtualStore.Domain.Entities.Produto

<div class="well">

    @if(Model.Imagem != null)
    {
        <div class="pull-left" style="margin-right: 10px">
            <img class="img-thumbnail" style="width:120px;height:120px;" src="@Url.Action("ObterImagem", "Produto", new { area = "Administrativo", Model.ProdutoId })" />
        </div>
    }

    <h3><strong>@Model.Nome</strong></h3>
    <h3><span class="pull-right label label-primary">@Model.Preco.ToString("c")</span></h3>

    @using(Html.BeginForm("Adicionar", "Carrinho"))
    {
        <div class="pull-right">
            @Html.HiddenFor(x => x.ProdutoId)
            @Html.Hidden("returnUrl", Request.Url.PathAndQuery)
            <input type="submit" class="btn btn-success" value="Comprar"/>
        </div>
    }

    <h3><span class="lead">@Model.Descricao</span></h3>
</div>